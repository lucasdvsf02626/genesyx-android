# Worklog — 2026-07-14

## 09:40 — Park uncommitted work

- Branch/HEAD: feature/partner-invites @ b7e4e56
- State: WIP
- Tests: NOT RUN at this commit (parking commit only; code unreviewed)
- Build: not run | release untouched
- Done:
  - Found ~887 lines of uncommitted work sitting in the tree on `feature/streaks-v2` (13 modified
    files, 6 untracked): partner invites via Supabase Edge Functions, and local daily-reminder
    notifications. Not in PR #9, not on `main`.
  - Parked it on `feature/partner-invites` rather than let it be swept into an unrelated branch or
    lost to a `git checkout .`. `feature/streaks-v2` / PR #9 left clean.
  - Correction to an earlier report in this session: I quoted "143 unit tests green" for PR #9.
    That was measured against the dirty tree and was wrong. PR #9 is **132**; the extra 11 came from
    this uncommitted work. CLAUDE.md's 132/17 was correct.
- Changed-meaning tests: none
- Open decisions for the human: whether this branch is ever finished or dropped
- Next: notifications audit (Part A)

## 10:05 — Notifications Part A (audit) + Part B (gap matrix)

- Branch/HEAD: feature/partner-invites @ b7e4e56
- State: WIP (read-only audit; no feature code written, per the hard stop)
- Tests: NOT RUN — this part changes nothing
- Build: not run | release untouched
- Done: audit + gap matrix below
- Changed-meaning tests: none
- Open decisions for the human: see "Rulings needed" at the end
- Next: HARD STOP — await approval of the gap matrix before any Part C code

### Audit basis (read this first)

The task says "invariant compliance TODAY". There are two different "todays":

| | Notification files | PUSH_NOTIFICATIONS | Can a notification fire? |
|---|---|---|---|
| `main` (shipped) | **0** | `false` | **No.** None of this exists. |
| `feature/streaks-v2` (PR #9, v1.1) | 0 | `false` | **No.** |
| `feature/partner-invites` (parked, unshipped) | 6 | `false` | Only if the flag is flipped |

So the audit below is of **176 lines of unreviewed, unshipped, flag-gated code** on the parked
branch. Nothing in it reaches a user today. Anything the gap matrix calls "EXISTS" exists only here.

### A1 — Scheduling entry points

| Entry point | Trigger | Time | Copy (verbatim) |
|---|---|---|---|
| `WorkManagerNotificationScheduler.scheduleDaily(enabled)` (`NotificationScheduler.kt:31`) | Only from `ProfileViewModel.setPush()` (`ProfileViewModel.kt:68`) — i.e. the user flipping the Profile toggle. Nothing else schedules. | `PeriodicWorkRequest`, 1 day, first fire at next **20:00** (`REMINDER_TIME`, `NotificationScheduler.kt:52`) | — |
| `ReminderWorker.doWork()` (`ReminderWorker.kt:33`) | The above periodic work | — | Title: **"How was today?"** Body: **"Take a moment to log how you're feeling."** (`ReminderWorker.kt:55-56`) |

That is the **entire** notification surface. One worker, one static string pair.

### A2 — Permission flow

`RemindersSection` (`ProfileScreen.kt:555-590`). Contextual, on toggle-on — good.
- API 33+: toggling on launches `POST_NOTIFICATIONS` (`:575`). Preference is only written **after**
  grant (`:563`). Never requested at launch.
- Denial: `denied` flag set (`:562`), switch stays off, explanatory text points at Settings (`:584`).
- Denial is **not persisted** — `var denied by remember` (`:557`) dies with the composable. Cosmetic
  only; the real state is re-derived from the system, so there is no re-prompt loop.
- Below API 33: plain switch, install-time permission (`:577`).

### A3 — Channels

One: `REMINDERS` = "Daily check-in", `IMPORTANCE_DEFAULT`, description "A once-a-day nudge to log
how you're feeling." (`NotificationChannels.kt:18-27`). No other channels exist.

`ensureCreated()` is called unconditionally in `GenesyxApplication.kt:14` — **not** gated on
`FeatureFlags.PUSH_NOTIFICATIONS`. So every user, including those on the shipped build where the
feature is off, gets a "Daily check-in" channel in Android system settings for a feature that cannot
fire. Minor, but it's a visible artefact of a disabled feature.

### A4 — Settings UI

A single on/off `SwitchRow("Daily check-in reminder")`, gated behind `FeatureFlags.PUSH_NOTIFICATIONS`
(`ProfileScreen.kt:210-213`) so it is **not visible in any shipped build**. No time picker. No
per-category anything.

### A5 — Invariant compliance (against the parked branch)

| # | Invariant | Verdict | Evidence |
|---|---|---|---|
| 1 | One push per day max | **PASS, by accident** | Only one category exists, and `NOTIFICATION_ID = 1001` is fixed (`ReminderWorker.kt:80`) so a repost replaces rather than stacks. There is **no ledger and no priority resolution**. The instant a second category is added this fails. |
| 2 | Never-guilt | **PASS** | The two strings are invitational. But there is no scan, and nothing prevents the next string from being an accusation. |
| 3 | Lock-screen privacy | **FAIL** | No `setVisibility()`, no `setPublicVersion()` anywhere. Channel gets platform default. Today's copy happens to be innocuous, so nothing leaks *yet* — but the **mechanism is entirely absent**, and invariant 3 explicitly says discreet-by-default, never rely on luck. |
| 4 | Per-category opt-in | **FAIL** | One channel, one toggle. And see the defect below — the toggle defaults to ON. |
| 5 | Quiet hours 21:00–08:00 | **FAIL** | Nothing enforces a window. It passes today only because `REMINDER_TIME` is hardcoded to 20:00. Worse: `PeriodicWorkRequest` is **inexact** — WorkManager may defer a job under Doze/battery constraints, so even the 20:00 slot can drift past 21:00 with nothing to stop it. |

### A6 — Defect found (real, not stylistic)

**The reminder toggle lies to new users.**

`pushEnabled` defaults to **`true`** — `GenesyxPreferencesDataStore.kt:46` (`it[Keys.PUSH] ?: true`)
and `PreferencesRepository.kt:30` (`Eagerly, true`). But work is only ever enqueued inside
`ProfileViewModel.setPush()`, which only runs when the user *touches* the switch.

So on a fresh install: Profile renders **"Daily check-in reminder: ON"**, no `POST_NOTIFICATIONS`
has ever been granted, no work has ever been enqueued, and **no reminder will ever arrive.** The
switch says on; the feature is off.

This directly contradicts the code's own doc comment at `ProfileScreen.kt:548` — *"the switch can
never sit 'on' while the system silently drops every notification"* — which is precisely what
happens on first run. It also breaks invariant 4 ("nothing fires before the user has enabled").

Fix is one character (`?: false`), but it belongs to whoever owns this branch. Flagging, not fixing.

### A7 — Dead / duplicated code

None found. No morning-nudge remnants; there is no second scheduler. The surface is too small to
have accumulated cruft.

### Part B — Gap matrix

Everything marked EXISTS in the task brief was verified against the code. Two of the brief's claims
about the existing reminder are **wrong**:

- Brief: *"user-set time (default 19:00)"* → **Reality: hardcoded 20:00, no picker anywhere.**
- Brief: *"two-branch: nothing meaningful logged → log invitation; else water < goal → hydration
  nudge"* → **Reality: no branching at all.** `ReminderWorker` posts the same static string pair
  unconditionally. It never reads a `DailyLog`, never checks water against the goal, and does not
  know whether she logged. It will cheerfully invite her to log a day she already logged.

| Category | Status | What's actually missing | Size | Invariant conflicts |
|---|---|---|---|---|
| Daily log reminder | **PARTIAL** | Content-awareness (the two-branch logic doesn't exist), user-set time, quiet hours, lock-screen treatment, default-ON defect (A6) | **M** | 3, 4, 5 |
| Streak milestone (7/14/30) | **MISSING** | `StreakEngine` computes `dailyActivity` — the signal exists; nothing observes it or fires on a threshold crossing. Needs a "already celebrated this milestone" ledger (`StreakState.celebrated` exists and may serve) | **M** | 1 (collision with daily reminder) |
| Weekly insights digest | **BLOCKED — cannot build** | **There is no Weekly Summary feature on Android.** Zero matches for `weeklySummary`/"weekly summary" in `app/src/main`. This is a P0 in the brief that depends on a feature that does not exist here. Either Weekly Summary ships first (its own feature, not a notification task) or the digest drops out of P0. | **L** (+ the whole missing feature) | 3 (must be discreet) |
| pH logging nudge | **MISSING** | `PhRepository` has the readings; nothing computes "days since last reading", no weekly cadence, no ledger | **S–M** | **3 — the sharpest.** A notification mentioning pH on a lock screen is exactly the disclosure invariant 3 exists to prevent. Copy must not say "pH". |
| Sleep pattern note | **MISSING** | Folded into the digest, so blocked on the same missing feature | **S** (once digest exists) | — |
| Cycle phase heads-up | **MISSING** | Cycle prediction exists (`domain/cycle`); no observer, no per-cycle ledger | **M** | **3 — severe.** Fertile-window/period timing on a lock screen. Off by default is right. |
| **NotificationGate** (the thing that makes all the above safe) | **MISSING** | Does not exist in any form. Ledger, priority resolution, quiet hours, category check, banned-phrase scan — all net-new. | **M–L** | It *is* the invariants |

**Priority order for same-day collision** — I agree with the brief's proposal and would ship it as:
`daily log reminder > streak milestone > cycle heads-up > weekly digest > pH nudge`, sleep never
competing (it lives inside the digest). One caveat: a 30-day streak milestone losing to a routine
daily nudge feels wrong. Suggest milestones outrank the daily reminder **on the milestone day only** —
it's rare, it's the single most positive message the app can send, and the daily nudge recurs
tomorrow at no cost. Human to rule.

**Android has no banned-phrase scan** to port — the brief was right to hedge. It is net-new, and it
is cheap: a pure function over a string list, unit-tested, called by the gate. Worth doing.

### Rulings needed before Part C

1. **Weekly digest is a P0 that can't be built.** Drop it, or commit to building Weekly Summary first?
2. **Milestone vs daily reminder priority** — accept the brief's order, or let milestones win on the day?
3. **Which P1s are in** (pH nudge / cycle heads-up / sleep line)?
4. **Who owns the A6 default-ON defect** — I flag it; the parked branch's author fixes it, or I do?
5. **Which branch does Part C build on?** The notification code is on `feature/partner-invites`,
   which is unreviewed WIP behind a flag that is off. Building the gate on top of it means Part C
   inherits an unreviewed base and cannot ship until that branch does.

---

## 11:15 — PR #9 merged; supplement card branch cut

- Branch/HEAD: feature/v1.2-supplement-card @ 3f933e7 (off main)
- State: WORKING
- Tests: unit **130/130** on merged main — measured, not quoted. CLAUDE.md says 132; it is off by
  two. (The 143 I reported earlier in the session was measured against the dirty tree and was wrong.)
- Build: not run at this point | release untouched
- Done:
  - Merged PR #9 (v1.1) to main. `main` = `3f933e7`. Nothing shipped to users — upload and promote
    are still owner actions, still gated on the three compliance items in CLAUDE.md.
  - Cut `feature/v1.2-supplement-card` off the new main. It needs `StreakEngine` and
    `hydrationGoalMl`, neither of which existed on main before the merge.
- Changed-meaning tests: none
- Open decisions for the human: none
- Next: Phase 0 — the supplement vocabulary

## 11:40 — Phase 0: one supplement vocabulary

- Branch/HEAD: feature/v1.2-supplement-card @ 183106c
- State: WORKING
- Tests: unit **136/136** (130 + 6 new `SupplementTest`)
- Build: assembleDebug OK | release untouched
- Done:
  - `domain/model/Supplement.kt` — five items, stable `id`, `displayName`, `dosageNote` held apart
    from the name. Default plan is folate/omega-3/vitamin D/zinc; **iron is loggable but outside the
    plan**, so it is recorded and never scored.
  - `NutritionContent.supplementPlan` and `LogScreen` now speak this one vocabulary. Zinc becomes
    loggable for the first time.
  - **The wire did not change.** Ids are internal; `Supplement.wireName` maps to the strings already
    stored. `daily_logs.supplements` is a shared `text[]` that iOS reads and writes, so renaming
    those strings would have scored every Android-logged day as zero on an iPhone and needed a
    production backfill. `SupplementTest` pins the four legacy names so a future tidy-up fails loudly.
- Changed-meaning tests: none. `NutritionContentTest` still asserts F/O/D/Z and passes unmodified —
  `SupplementPlanItem.name` recomposes the same display strings from the split fields.
- Open decisions for the human:
  - **Tell iOS about Zinc.** Making it loggable adds one new string ("Zinc") to the shared column.
    This is **additive** — nothing is renamed, no existing row is touched — but iOS will start seeing
    a value it doesn't know. It should ignore it gracefully; worth a message to whoever owns it.
  - **Iron vs the plan, on device.** Iron now sits in the Log but not in the Nutrition plan stack.
    Nothing reads wrong, but a woman taking iron may wonder why it isn't in "her plan". If that looks
    odd in review, the fix is either to add iron to the plan (one line) or to pull plan-editing
    forward. Not decided here.
  - **Copy constraint not met, deliberately.** The brief said "strings in resources". The existing
    insight logic (`HydrationInsightLogic`, `ConsistencyInsightLogic`, `PhInsightLogic`) all return
    plain Kotlin strings, because they are pure and `Context`-free — which is exactly what makes them
    unit-testable without Robolectric. Moving copy to resources would mean threading a `Context` into
    pure domain logic and would break that. I followed the existing convention. Flagging rather than
    doing it silently; if you want resources, it's a separate change across all four logic objects.
- Next: Phase 1 — kill the duplicated week logic

## 12:00 — Phase 1: one week-bucketing implementation

- Branch/HEAD: feature/v1.2-supplement-card @ 5c037b2
- State: WORKING
- Tests: unit **141/141** (+5 `WeekBucketsTest`: Monday, Sunday, new year, leap day, ordering)
- Build: assembleDebug OK | release untouched
- Done:
  - `domain/time/WeekBuckets.kt` — the single ISO Monday-start definition. `HydrationInsightLogic`
    and `StreakEngine` both migrated. Net copies of week logic in the codebase: **one**.
- Changed-meaning tests: **none, and that is the point.** `StreakEngineTest` (27),
  `TrackingVectorTest` (3) and `HydrationInsightLogicTest` (8) all pass **unmodified** — that is the
  proof the refactor preserved behaviour. The vectors matter most: they are mirrored verbatim into
  the iOS repo, so drift here would have failed the build on both platforms.
- Open decisions for the human: none
- Next: Phase 2 — the card

## 12:30 — Phase 2: the card, and the hydration-goal bug

- Branch/HEAD: feature/v1.2-supplement-card @ 5e900a6
- State: WORKING
- Tests: unit **151/151, 0 failures** (+9 `SupplementInsightLogicTest`, +1 hydration-goal test)
- Build: **assembleDebug OK, assembleRelease OK (R8/minify clean)**
- Done:
  - `ui/insights/SupplementInsightLogic.kt` (pure, follows the real `ui/insights/*InsightLogic.kt`
    convention — there is no `domain/insights/`), `SupplementInsights` alongside the other state
    types in `InsightsViewModel`, `SupplementCard` in `InsightsScreen` reusing the existing
    `BarsCard` and `StreakTile`. Placed directly after Hydration.
  - Denominator is her **plan** (4), not the 5 she can tick. Whole plan + iron = 100%, not 125%.
  - **Fixed the hydration-goal bug found during the fact-check.** `HydrationInsightLogic` held a
    private `GOAL_ML = 2400` that shadowed the goal she had actually set — a woman on a 3000ml goal
    had her Insights bars scored against 2400. The two are equal by default, which is why nobody saw
    it. `compute()` now takes her goal; `InsightsViewModel` reads it from `PreferencesRepository`.
    This is the "v1.1 goal hotfix" the brief assumed was already merged. **It was not.** It is now.
- Changed-meaning tests: none removed. One added to `HydrationInsightLogicTest` pinning the fix.
- Open decisions for the human: the three from Phase 0 above (Zinc → iOS; iron-outside-plan on
  device; copy-in-resources), plus on-device review of the card.
- Next: on-device check by the human, then Notifications Part C on a fresh branch off main

### Manual check for the human (5 minutes, on device)

1. Log → Supplements: **five** items now (Zinc is new). Tick Folic acid + Zinc → save.
2. Insights: the Supplements card sits under Hydration. Today's bar reads **50%** (2 of a 4-item
   plan). Tiles: "Days logged 1/7", "Supplements taken 2".
3. Tick **Iron as well** → save. The bar stays **50%**, and "Supplements taken" stays **2**. Iron is
   recorded, not scored — that is the intended behaviour, not a bug.
4. Tick all four plan items → bar reads **100%**.
5. Profile → set the hydration goal to 3000ml. Log 1500ml. The **Hydration** card's bar for today
   should read **50%**, not 62%. Before this branch it read 62% — that was the bug.
